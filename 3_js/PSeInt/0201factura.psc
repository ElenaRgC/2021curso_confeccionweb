Algoritmo factura
	
	// Arriba: nombre empresa, número factura
	
	// Cuerpo: cantidad, artículo, precio por unidad, importe.
	// Totales: importe bruto (suma todos los importes), descuentos, impuestos (21%), neto
	
	// Vencimiento: introduces fecha > tipo pago (X días) > vence el día Y.
	// 30, 60, 90 días.
	
FinAlgoritmo
